# templates/

The `templates/` directory contains reusable structures for creating consistent HCOS™ documents, decisions, and change proposals.

Templates should be adapted proportionally. Remove sections that do not apply rather than filling them with unnecessary content.
